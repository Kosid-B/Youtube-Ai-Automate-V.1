/**
 * ความรู้สำหรับให้ AI คิดหัวข้อคลิป
 *
 * รวมจาก 4 แหล่งที่เจ้าของโปรเจครวบรวมไว้เป็น skill:
 * - market-insight-thailand  → ใครคือคนดู (ข้อมูลประชากรจริง ทะเบียนราษฎร์ ธ.ค. 2568)
 * - ai-dark-marketing        → อะไรทำให้คนหยุดเลื่อน (จิตวิทยาการตัดสินใจ)
 * - marketplace-seo          → คนพิมพ์ค้นด้วยคำแบบไหน
 * - market-research          → มีคนอยากรู้เรื่องนี้จริงไหม
 *
 * ⚠️ เขียนเป็นค่าคงที่ในโค้ด ไม่ใช่ให้โมเดลนึกเอง เพราะตัวเลขประชากรกับสัดส่วนเจน
 * เป็นข้อเท็จจริงที่โมเดลจำผิดได้ และผิดแล้วจะกลายเป็นหัวข้อที่เล็งกลุ่มผิด
 */

import { salesStyleContext, type ScriptStyle } from '@/lib/sales-style'
import type { ProofPoint } from '@/lib/proof'

/** สัดส่วนเจนจากทะเบียนราษฎร์ ธ.ค. 2568 (skill: market-insight-thailand) */
export const AUDIENCE_SEGMENTS = [
  {
    key: 'gen-x',
    label: 'Gen X',
    share: 24,
    role: 'หัวหน้าครอบครัว เป็นผู้ตัดสินใจซื้อ',
    speakTo: 'เน้นความน่าเชื่อถือและความจริงใจ ไม่ชอบคำโฆษณาเกินจริง',
  },
  {
    key: 'gen-y',
    label: 'Gen Y',
    share: 23,
    role: 'พนักงานประจำและเจ้าของธุรกิจขนาดเล็ก',
    speakTo: 'เน้นความคุ้มค่าและผลลัพธ์ที่วัดได้ อยากเห็นตัวเลข',
  },
  {
    key: 'gen-z',
    label: 'Gen Z',
    share: 20,
    role: 'กำลังซื้อในอนาคต เพิ่งเริ่มทำงานหรือเริ่มธุรกิจ',
    speakTo: 'เน้นความเร็วและความจริง ไม่ทนกับการเกริ่นยาว',
  },
] as const

/** จังหวัดกำลังซื้อสูง — ใช้ทำหัวข้อเฉพาะพื้นที่ซึ่งแข่งน้อยกว่าหัวข้อกลาง ๆ */
export const HIGH_VALUE_PROVINCES = [
  'กรุงเทพฯ', 'นครราชสีมา', 'อุบลราชธานี', 'เชียงใหม่', 'ขอนแก่น',
  'ชลบุรี', 'บุรีรัมย์', 'อุดรธานี', 'นครศรีธรรมราช', 'ศรีสะเกษ',
] as const

/**
 * มุมเปิดเรื่องที่ทำให้คนหยุดเลื่อน
 *
 * มาจาก skill ai-dark-marketing ซึ่งอธิบายกลไกการตัดสินใจของคน
 * ⚠️ ตัว skill นั้นกำกับไว้เองว่า "ควรนำไปใช้อย่างโปร่งใสและคำนึงถึงจริยธรรม"
 * จึงเก็บเฉพาะมุมที่ยังพูดความจริงได้ และตัดมุมที่ต้องโกหกถึงจะใช้ได้ออก
 * (เช่น ความขาดแคลนปลอม นับถอยหลังปลอม จำนวนคนซื้อปลอม)
 */
export const HOOK_ANGLES = [
  {
    key: 'loss',
    label: 'สิ่งที่กำลังเสียอยู่โดยไม่รู้ตัว',
    why: 'คนตอบสนองต่อการเสียมากกว่าการได้ในขนาดเท่ากัน',
    example: 'กำไรหายไปทุกออร์เดอร์โดยที่ไม่มีใครขโมย',
  },
  {
    key: 'number',
    label: 'ตัวเลขเฉพาะเจาะจง',
    why: 'ตัวเลขที่ไม่กลมทำให้รู้สึกว่ามาจากการวัดจริง ไม่ใช่การเดา',
    example: 'ต้นทุนต่อจานที่คุณคิดอยู่ ยังไม่ได้หัก 7 อย่าง',
  },
  {
    key: 'contrarian',
    label: 'สวนความเชื่อที่คนส่วนใหญ่ยึด',
    why: 'ขัดกับสิ่งที่คิดไว้แล้วทำให้ต้องหยุดฟังเหตุผล',
    example: 'ลดราคาแล้วยอดขึ้น แต่ทำไมเจ๊งเร็วกว่าเดิม',
  },
  {
    key: 'mistake',
    label: 'ความผิดพลาดที่พบบ่อย',
    why: 'คนอยากรู้ว่าตัวเองอยู่ในกลุ่มที่พลาดหรือเปล่า',
    example: 'สามข้อที่ SME ทำเหมือนกันหมดก่อนขาดสภาพคล่อง',
  },
  {
    key: 'howto',
    label: 'ทำตามได้ทันที',
    why: 'สัญญาผลลัพธ์ที่ชัดและตรวจสอบได้ด้วยตัวเอง',
    example: 'คำนวณต้นทุนจริงของสินค้าตัวเดียวใน 30 นาที',
  },
  {
    key: 'story',
    label: 'เรื่องจริงของธุรกิจหนึ่งราย',
    why: 'คนจำเรื่องเล่าได้ดีกว่าข้อสรุป',
    example: 'ร้านที่ยอดขายโตทุกเดือนแต่จ่ายเงินเดือนไม่ได้',
  },
] as const

/**
 * เส้นที่ห้ามข้าม
 *
 * หัวข้อที่เร้าใจแต่เนื้อหาให้ไม่ได้ = คนกดเข้ามาแล้วออกทันที
 * YouTube วัดสัดส่วนการดูจบ คลิปแบบนั้นจึงถูกลดการกระจายอยู่ดี —
 * นอกจากเสียความน่าเชื่อถือแล้วยังไม่ได้ผลด้วยซ้ำ
 *
 * ข้อสุดท้ายมาจากกติกา data-verification ของโปรเจคเอง
 */
export const HOOK_RULES = [
  'หัวข้อต้องเป็นสิ่งที่เนื้อหาในคลิปทำได้จริง ห้ามสัญญาเกินกว่าที่จะเล่าได้',
  'ห้ามสร้างความเร่งด่วนปลอม เช่น "เหลือเวลาอีก 3 วัน" ที่ไม่มีอยู่จริง',
  'ห้ามใส่ตัวเลขที่ยังไม่ได้ตรวจแหล่งที่มา — ถ้าไม่มีตัวเลขจริงให้ใช้มุมอื่นแทน',
  'ห้ามอ้างว่าเป็นความลับหรือสิ่งที่ "ไม่มีใครบอกคุณ" ถ้ามันเป็นความรู้ทั่วไป',
] as const

export type IdeaBrief = {
  channelName: string
  niche: string | null
  /** หัวข้อที่ทำไปแล้ว — กันคิดซ้ำ */
  recentTitles: string[]
  /** โทนการเล่าของช่อง — โทน direct ทำให้หัวข้อต้องสัญญาผลลัพธ์ที่จับต้องได้ */
  style?: ScriptStyle
  /** หลักฐานที่ช่องอ้างได้ — หัวข้อที่มีตัวเลขต้องมาจากรายการนี้เท่านั้น */
  proof?: ProofPoint[]
  /** เจนที่อยากเจาะ ไม่ระบุ = ให้ AI เลือกเองตามแนวช่อง */
  segment?: (typeof AUDIENCE_SEGMENTS)[number]['key']
  count: number
}

/**
 * ประกอบบริบทให้โมเดล
 *
 * ใส่ข้อมูลประชากรจริงเข้าไปด้วย ไม่ปล่อยให้โมเดลนึกเอง — ตัวเลขพวกนี้
 * โมเดลจำผิดได้ง่าย และผิดแล้วหัวข้อจะเล็งกลุ่มคนผิดตั้งแต่ต้น
 */
export function ideaContext(brief: IdeaBrief): string {
  const style = salesStyleContext(brief.style ?? 'informative', brief.proof ?? [])

  const segments = brief.segment
    ? AUDIENCE_SEGMENTS.filter((s) => s.key === brief.segment)
    : AUDIENCE_SEGMENTS

  return [
    `ช่อง: ${brief.channelName}`,
    brief.niche ? `แนวเนื้อหา: ${brief.niche}` : null,
    '',
    'คนดูในไทย (ทะเบียนราษฎร์ ธ.ค. 2568):',
    ...segments.map((s) => `- ${s.label} ${s.share}% · ${s.role} · สื่อสาร: ${s.speakTo}`),
    '',
    `จังหวัดกำลังซื้อสูง: ${HIGH_VALUE_PROVINCES.join(', ')}`,
    '',
    'มุมเปิดเรื่องที่ใช้ได้:',
    ...HOOK_ANGLES.map((h) => `- ${h.label} (${h.why}) เช่น "${h.example}"`),
    '',
    'ข้อห้าม:',
    ...HOOK_RULES.map((r) => `- ${r}`),
    '',
    // โทนของช่องต้องมาถึงตั้งแต่ตอนคิดหัวข้อ ไม่ใช่ไปเริ่มตอนเขียนบท —
    // หัวข้อที่คิดมาแบบเล่าเปล่า ๆ ดัดให้เป็นโทนชวนลงมือทีหลังไม่ได้
    style ? `${style}\n` : null,
    brief.recentTitles.length > 0
      ? `หัวข้อที่ช่องนี้ทำไปแล้ว (ห้ามซ้ำและห้ามใกล้เคียง):\n${brief.recentTitles.map((t) => `- ${t}`).join('\n')}`
      : null,
    '',
    `เสนอมา ${brief.count} หัวข้อ`,
  ]
    .filter((line) => line !== null)
    .join('\n')
}
